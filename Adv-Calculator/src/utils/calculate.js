// OWNER: Daniel (Math Brain)
//
// This file holds the PURE MATH functions for the calculator.
// "Pure" means: no React, no UI, no state — just functions that take
// numbers in and give numbers out. This makes them easy to test on their own.
//
// TODO:
// - add(a, b)
// - subtract(a, b)
// - multiply(a, b)
// - divide(a, b)        -> must handle divide-by-zero safely (return an "Error" state, not Infinity)
// - percentage(value, base) -> decide: simple (value / 100) or context-aware (base * value / 100)
// - a main "calculate(a, b, operator)" function that picks the right operation above
