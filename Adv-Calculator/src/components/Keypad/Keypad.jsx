// OWNER: David (The Face)
//
// This component renders the full button grid: 0-9, decimal, operators,
// equals, clear, backspace, +/-.
//
// Import useCalculator() from src/hooks/useCalculator.js (built by Daniel)
// and call its handler functions on each button's onClick.
// Do NOT write any math or state logic here — just call the handlers.
//
// TODO:
// - lay out buttons using CSS Grid (see Keypad.module.css)
// - map each button to the correct handler:
//     0-9      -> handleDigit(digit)
//     .        -> handleDecimal()
//     + - × ÷  -> handleOperator(operator)
//     %        -> handlePercentage()
//     +/-      -> handleToggleSign()
//     =        -> handleEquals()
//     C        -> handleClear()
//     backspace -> handleDelete()
